# === AWS CLI / 배포용 환경변수 ===
# 리전 (예: 서울)
AWS_REGION=ap-northeast-2

# AWS 계정 ID (12자리 숫자)
AWS_ACCOUNT_ID=842903729788

# AWS CLI 프로파일 (기본값이면 default)
AWS_PROFILE=default

# EC2 키페어 이름 (create-key-pair에서 생성)
KEY_NAME=postiz-key

# EC2 인스턴스 타입
INSTANCE_TYPE=t3.small

# 보안그룹 이름
SG_NAME=postiz-sg

# 인스턴스 Name 태그
TAG_NAME=postiz-ec2

# SSH 허용 CIDR (예: 1.2.3.4/32)
# 보안을 위해 본인 공인 IP로 제한 권장
SSH_CIDR=
